export interface dataType {
    title: string;
    smallDescription: string;
    image: any;
    currentSlug: string;
};

export interface fullBlogType {
    currentSlug: string
    title: string
    content: string | any
    image: any
}